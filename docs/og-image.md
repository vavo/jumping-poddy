# Social sharing image

The page uses `public/og-jumping-poddy.png` for Open Graph and Twitter summary cards. Production metadata points to `https://forma.cam/runpod/og-jumping-poddy.png`.

Created with the built-in image-generation tool using the supplied official Poddy image as the character reference. The composition is promotional artwork, not a gameplay screenshot.

Image dimensions: 1734 × 907 pixels (PNG).

## Generation prompt

Use case: ads-marketing. Create a finished cute premium 3D social sharing / Open Graph card for the browser game "Jumping Poddy", using the attached image as the exact mascot identity reference. Wide landscape composition, 1.91:1 aspect ratio, target 1200 x 630 pixels. Poddy is a small rounded violet computer pod with a dark inset screen, two pale lavender vertical capsule eyes, a friendly curved smile, three horizontal front ventilation slots, chunky side panels and a tiny base; preserve those signature features. No arms or legs. He is joyfully bouncing over a tiny detailed GPU and a sealed hardware box, mint-green microchip collectibles sparkling in a gentle arc. Soft plush-like rounded proportions while retaining a polished purple plastic case, adorable expressive face. Place Poddy large on the right with a dynamic friendly tilt; keep a clean left typography area. A stylized purple datacenter runway recedes into soft-focus server racks in the background. Deep midnight violet, luminous lavender and mint green palette. Beautiful soft cinematic lighting, ambient occlusion, subtle glow, highly legible at thumbnail size. Large bold condensed slightly italic warm-white title on the left in two lines, exact text: "JUMPING" then "PODDY". Below in smaller clean type, exact text: "Big compute. Tiny pod." Small mint rounded label reading "PLAY IN YOUR BROWSER". Keep all text comfortably inside a 60px safe margin, polished graphic design hierarchy, no tiny clutter, no watermark, no invented logos. This is a new promotional composition, not a screenshot or UI mockup.

Keep image dimensions and alt text in `index.html` consistent when replacing the artwork. Rebuild and deploy the page and image together before checking a public link preview; pushing to GitHub alone does not update the live site.
